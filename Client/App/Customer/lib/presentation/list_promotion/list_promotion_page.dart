import 'package:flutter/material.dart';
import 'package:user/presentation/list_promotion/list_promotion_view.dart';

class ListPromotionPage extends StatelessWidget {
  const ListPromotionPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ListPromotionView(),
    );
  }
}
